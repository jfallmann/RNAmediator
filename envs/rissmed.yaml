name:
  rissmed
channels:
  - conda-forge
  - defaults
  - bioconda
dependencies:
  - python>=3.9
  - viennarna>=2.4.18
  - pip
  - pip:
    - numpy
    - biopython
    - pandas
    - natsort
    - dash
    - dash-bootstrap-components
